# 防风控策略

> 模拟真人阅读节奏，避免被微信读书风控系统识别为自动化行为

---

## 一、策略总览

| 策略 | 参数 | 说明 |
|------|------|------|
| 启动随机延迟 | 30~90s | 点击"开始阅读"后不立即请求，模拟真人准备 |
| 请求间隔随机化 | 30~45s ± 15% | 基础间隔 + 抖动系数，避免固定节奏 |
| 长休息 | 60~180s | 每 20 次阅读插入一次长休息，模拟疲劳 |
| 失败冷却 | 30~60s | 请求失败后额外等待，避免连续重试 |
| 每日时长限制 | 1.5~3h | 避免单日超长阅读（>6h 平台可能打折不计） |
| 书籍多样性 | 20~40 次换书 | 避免长时间只读一本书 |
| 章节多样性 | 5 次换章 | 避免同一章节连续使用过多 |
| 健康巡检 | 3~12 分钟 | 定期检查登录态，避免失效后疯狂重试 |

---

## 二、配置参数

文件位置：[app/core/config.py](file:///e:/item/wxread/app/core/config.py#L54-L74)

```python
"reading": {
    "min_hours": 1.5,                    # 每日最少小时数（风控建议：1.5~3h）
    "max_hours": 3,                       # 每日最多小时数（>6h 高危）
    "min_interval_sec": 30,               # 请求间隔下限（风控建议：30~45s）
    "max_interval_sec": 45,               # 请求间隔上限
    "startup_delay_min_sec": 30,          # 启动随机延迟下限
    "startup_delay_max_sec": 90,          # 启动随机延迟上限
    "switch_book_every_min": 20,          # 换书次数下限
    "switch_book_every_max": 40,          # 换书次数上限
    "same_chapter_max_reuse": 5,          # 同一章节最大连续使用次数
    "health_check_first_min": 3,          # 首次健康巡检间隔（分钟）
    "health_check_min": 12,               # 之后每多少分钟一次巡检
    "book_switch_recent_exclude": 3,      # 换书时排除最近 N 本
    "chapter_switch_recent_exclude": 5,   # 换章时排除最近 N 章
},
```

---

## 三、策略详解

### 1. 启动随机延迟

**目的**：避免点击"开始阅读"后立即秒级请求，模拟真人进入阅读前的准备时间。

文件位置：[app/core/scheduler.py](file:///e:/item/wxread/app/core/scheduler.py#L149-L170)

```python
# 启动随机延迟（反机器指纹）
startup_delay = random.uniform(30, 90)
log.info("⏱️ 启动随机延迟：%.1fs（30~90s），模拟真人进入阅读前的思考准备", startup_delay)

# 分段 sleep，确保 stop/pause 可打断
while _dl > 0 and not self._stop_event.is_set():
    chunk = min(0.2, _dl)
    time.sleep(chunk)
    _dl -= chunk
```

### 2. 请求间隔随机化

**目的**：避免固定节奏的请求，模拟真人翻页的不均匀性。

文件位置：[app/core/scheduler.py](file:///e:/item/wxread/app/core/scheduler.py#L216-L247)

```python
# 基础间隔：30~45 秒随机
base_interval = random.uniform(30, 45)

# ±15% 抖动系数
jitter = random.uniform(0.85, 1.15)
cur_interval = base_interval * jitter

# 日志示例：
# 📏 阅读间隔：间隔=30~45s | 基础=37.2s | 抖动×0.93 → 最终=34.6s
# 📏 阅读间隔：间隔=30~45s | 基础=41.8s | 抖动×1.12 → 最终=46.8s
```

### 3. 长休息（每 20 次）

**目的**：模拟真人阅读疲劳后的停顿，每 20 次请求插入一次较长休息。

```python
# 每 20 次成功阅读加一段较长休息
if plan.success_count > 0 and plan.success_count % 20 == 0:
    break_extra = random.uniform(60, 180)
    cur_interval += break_extra
    # 日志：☕ 每 20 次长休息：+112s（下一轮 ≈ 145.3s）
```

### 4. 失败冷却

**目的**：请求失败后额外等待，避免连续重试触发风控。

```python
# 失败时加 30~60s 额外冷却
if not ok:
    fail_extra = random.uniform(30, 60)
    cur_interval += fail_extra
    # 日志：⚠️ 本次阅读失败，额外冷却 47s（下一轮 ≈ 77.1s）
```

### 5. 每日时长限制

**目的**：避免单日超长阅读，微信读书平台对 >6h 的超长段可能打折不计。

```python
# 每日目标在 [1.5, 3] 小时区间随机
# 风控建议：1.5~3h，避免 >6h 单日高危
"min_hours": 1.5,
"max_hours": 3,
```

### 6. 书籍/章节多样性

**目的**：避免长时间只读一本书或一个章节。

```python
# 换书：每 20~40 次阅读随机切换书籍
"switch_book_every_min": 20,
"switch_book_every_max": 40,

# 换章：同一章节连续使用超过 5 次强制切换
"same_chapter_max_reuse": 5,

# 排除最近读过的书/章节，避免重复
"book_switch_recent_exclude": 3,
"chapter_switch_recent_exclude": 5,
```

### 7. 健康巡检

**目的**：定期检查登录态，避免 Cookie 失效后疯狂重试。

```python
# 启动后 3 分钟首次巡检
"health_check_first_min": 3,

# 之后每 12 分钟一次巡检
"health_check_min": 12,
```

---

## 四、完整间隔计算示例

```
场景1：正常阅读（第 5 次）
  base = 37.2s (random 30~45)
  jitter = 0.93 (random 0.85~1.15)
  interval = 37.2 × 0.93 = 34.6s

场景2：每 20 次长休息
  base = 41.8s
  jitter = 1.12
  break_extra = 112s (random 60~180)
  interval = 41.8 × 1.12 + 112 = 158.8s

场景3：失败冷却
  base = 35.5s
  jitter = 0.88
  fail_extra = 47s (random 30~60)
  interval = 35.5 × 0.88 + 47 = 78.2s

场景4：长休息 + 失败
  base = 42.0s
  jitter = 1.05
  break_extra = 95s
  fail_extra = 52s
  interval = 42.0 × 1.05 + 95 + 52 = 191.1s
```

---

## 五、Skill 限流保护

```python
# HTTP 499 = 限流
if r.status_code == 499:
    return None  # 立即返回，不重试

# 阅读统计缓存 TTL = 180 秒，避免频繁调用
"summary_cache_ttl": 180,

# 批量获取进度时间隔 300ms
time.sleep(0.3)
```

---

## 六、风险提示

> ⚠️ 本项目仅供学习交流，自动化行为可能触发风控

1. **频率风险**：过于频繁的请求可能导致账号被限制
2. **Cookie 过期**：`wr_skey` 有效期有限，需定期重新登录
3. **IP 风险**：长时间固定 IP 可能触发风控，建议配合代理使用
4. **Skill Key**：`wrk-*` 开头的 API Key 需妥善保管

---

## 七、推荐配置

| 场景 | 每日时长 | 间隔 | 换书频率 |
|------|---------|------|---------|
| 保守 | 1.5~2h | 40~60s | 30~50 次 |
| 标准 | 2~3h | 30~45s | 20~40 次 |
| 激进 | 3~4h | 25~35s | 15~25 次 |

**建议使用"标准"配置，在效率和安全性之间取得平衡。**
