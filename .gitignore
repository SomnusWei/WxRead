# ===== 安全：含 GitHub Personal Access Token / 账号密码的本地备忘 =====
# 绝对不能提交（GitHub 机器人会扫描并自动吊销，导致账号受损）
github项目更新需要.txt
*PAT*.txt
*token*.txt
*credential*.txt

# ===== Python 缓存 =====
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/

# ===== 虚拟环境 =====
.venv/
venv/
env/
ENV/

# ===== PyInstaller 构建产物（超大二进制，重建即可）=====
# dist/: 打包好的 EXE + 全部运行时（本项目约 580MB）
dist/
# build/: PyInstaller 中间缓存（PYZ/Toc/pkg，约 20MB）
build/
# .spec 在本项目是手写构建配置，保留

# ===== 运行时日志（按项目机制：APPDATA 保留 14 天，项目根保留 3 天）=====
app.log
*.log.*.txt
errorlog/
logs/

# ===== 临时 smoke / debug / 临时测试输出 =====
*smoke*.log
*smoke*_stderr.log
*smoke*_stdout.log
*_stderr.log
*_stdout.log
*_err.txt
*_out.txt
build_out.log
build_err.log
pyinstaller.log
build_rc.txt
*.bak
*.tmp
*.swp

# ===== IDE =====
.vscode/
.idea/
.trae/
*.DS_Store
Thumbs.db

# ===== 本地临时配置（config.json 存于 %APPDATA%，但如果有人误放根目录也不能提交）=====
config.json
settings.json

# ===== 临时测试文件 =====
cdp_test.log
cdp_test_simple.py
test_extension.log

# ===== 临时快照 =====
cdp_cookies_snapshot.json

# ===== 构建临时目录 =====
build5/
build_cdp/
build_ext/
dist5/
dist_cdp/
dist_ext/
backup_exe/

# ===== 测试扩展 =====
test_extension/

# ===== 临时文档 =====
微信读书自动读书APP-书架与章节池获取方案.md
阅读任务-章节池持久化方案(纯Web-Cookie-无Skill).md
CDP重构方案.md
现有官方数据卡从Skill获取数据的核心.md
微信读书自动阅读开发指南-bookId与chapterUid获取.md
项目计划.txt

# ===== 启动 / smoke 临时追踪日志 =====
start_trace*.log

# ===== release/：只保留 release_note-v*.md（文档），大二进制不进 git =====
# onedir 展开目录（数百 MB 到 GB 级）
release/WxReadAssistant-*-full/
# 完整安装包 zip
release/WxReadAssistant-*-full.zip
# sha1 校验清单（二进制辅助）
release/WxReadAssistant-*-full.sha1.txt
# 增量补丁 zip
release/patch-*.zip
# Windows 安装器（setup.exe + 校验）
release/WxReadAssistant-*-setup.exe
release/WxReadAssistant-*-setup.sha1.txt
release/WxReadAssistant-*-iconfix-*.exe
release/WxReadAssistant-*-iconfix-*.sha1.txt

# ===== Docker 版（wxread_docekr）敏感/本地文件，绝对不能提交 =====
# NAS SSH 凭据（devtools 运维工具的登录信息）
测试nas部署ssh信息.txt
*ssh信息*.txt
# NAS 内网运维工具（含内网拓扑，仅本地使用）
wxread_docekr/devtools/
# 内部验收报告（含真实 Skill key / 内网 IP / 设备 ID）
wxread_docekr/ACCEPTANCE.md
# 运行时数据目录（含真实 Cookie / 授权码 / 配置）
wxread_docekr/tests/_tmp*/
wxread_docekr/tests/_shots/
wxread_docekr/_probe*
wxread_docekr/**/*.log
wxread_docekr/.env
# 容器构建产物
wxread_docekr/**/__pycache__/
